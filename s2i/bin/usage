#!/bin/bash -e
cat <<EOF
This is the anaconda-project-centos7 S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> anaconda-project-centos7 <application image>

You can then run the resulting image via:
docker run -p 8086:8086 <application image>
EOF
